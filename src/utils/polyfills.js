if (window && !window.console) {
    if (window && !window.console) {
        window.console = {
            log: function () {
            },
            warn: function () {
            },
            error: function () {
            }
        };
    }
    window.console = {
        log: function () {
        },
        warn: function () {
        },
        error: function () {
        }
    };
}